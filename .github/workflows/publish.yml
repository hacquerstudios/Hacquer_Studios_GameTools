name: Publish NuGet Package

on:
  workflow_dispatch:
  push:
    branches:
      - release

jobs:
  publish:
    runs-on: ubuntu-latest

    permissions:
      id-token: write      # REQUIRED for OIDC handshake
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build -c Release

      - name: Pack
        run: dotnet pack -c Release -o out

      - name: Publish to NuGet (OIDC)
        run: dotnet nuget push "out/*.nupkg" --api-key "GitHub" --source "https://api.nuget.org/v3/index.json" --skip-duplicate

